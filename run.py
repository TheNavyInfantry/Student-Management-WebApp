from student_management import app  # variable called app comes from __init__

if __name__ == "__main__":  # Reminder for myself=>Checks if the run.py has executed directly and not imported
    app.run(debug=True)